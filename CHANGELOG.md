# Changelog

## 0.1.7 (2026-09-18)

- **新增商店横幅 banner（SVG 设计，1280×540 = 64:27）**：浅色背景高对比（标题
  对比度约 12:1，满足上架审核要求），左侧标题区 + 右侧芯片扇出网络插画（延续
  logo 视觉语言），文字全部矢量渲染；源码 `images/banner.svg` 随仓库分发。

## 0.1.6 (2026-09-18)

- **更换插件 logo 为 SVG 设计稿**：深藏青底 + 中央 MCU/FPGA 芯片(U1 位号) + 左右引脚
  扇出三色网络导线(PA0/PA1/PB6、PB0/PB7/RX1 网络标签胶囊)，直接图示插件功能；
  SVG 源码随仓库分发（`images/logo.svg`），由 Chrome 无头模式渲染为 1024×1024 PNG。

## 0.1.5 (2026-09-16)

- **左/上引出标签与导线协同修正**（实测确认：标签锚点=文字左下角，且锚点必须
  落在导线上，否则网络绑定丢失——v0.1.4 把锚点移到线外导致"线没接触到网络"）：
  - 标签锚定改为导线**外端点**，文字沿导线向引脚方向延伸、右缘抵住引脚；
  - 左/上引出的导线按网络名文字宽度**动态延长**（估算宽 + 边距，网格向上取整，
    默认长度不够时才延长），保证文字全程压线、网络绑定不断；
  - 右/下引出保持 v0.1.4 行为（锚点=引脚端，文字向线外延伸）。

## 0.1.4 (2026-09-16)

- **标签锚点改为导线起点（引脚端）**：v0.1.3 以导线末端为参照，实测两侧都不对
  （标签漂到线外端）。现以**导线起点=引脚端点**为参照：右/下引出文字左下角锚定
  在引脚处、沿引出方向向外延伸；左/上引出按网络名估宽把文字右缘锚定在引脚处、
  向外（左）延伸，等效右下角锚定。标签紧贴各自引脚成列对齐。

## 0.1.3 (2026-09-16)

- **标签分侧定位**：原生虚拟网络标签的渲染偏移是固定的，左侧引出会压线/压引脚、
  右侧引出线端到文字之间留空。新增「标签定位」设置（默认分侧对齐）：右/下侧
  引出的标签锚定在导线末端、文字向外延伸（等效左下角锚定）；左/上侧引出按网络名
  估算文字宽度左移锚点，使文字在导线末端向外结束（等效右下角锚定），两侧标签
  成列对齐。v4 用 createNetLabel 直接落位；v3.2 经属性图元 modify 移动原生标签
  （sch_PrimitiveAttribute.getAll(导线ID) 匹配网络名 -> modify x），客户端不支持
  时自动保持编辑器原生位置（可在设置切回"编辑器原生位置"对比）。

## 0.1.2 (2026-09-16)

实测二轮反馈修复（方向误判 / 短桩 / 端口全灭 / 限流慢）：

- **引脚朝向主判据改为 rotation 分组**：同侧引脚共享同一 rotation，按 rotation
  分组后以组质心相对整体中心的主轴偏移定方向——不依赖 rotation 角值约定，
  天然兼容器件旋转/镜像与参差引脚列（点云边缘法对后者失效）；点云边缘
  归属降为交叉校验（高置信边缘可纠正个别画错的 rotation）。
- **新增「引出方向校正」设置**（自动/左右互换/上下互换/旋转 180°）：
  个别符号样式判向不符合预期时一键纠正，无需发版。
- **修复跟随鼠标短桩**：锚点在器件内部/贴边时，旧钳位只退 1 格产出 5 单位
  短桩；现在钳位保证标签列离器件至少"引出导线长度"（默认 30 单位）。
- **网络标签/端口熔断**：连续创建失败 5 次自动停试（实测 V3.2.149 的
  createNetPort 全军覆没），余下项目仅创建导线，完成弹窗提示改回"自动"模式。
- **自适应节奏**取代固定间隔+固定退避：起步 100ms（可设 0=不限速），失败
  节奏翻倍（封顶 1.2s）、连续 4 次成功回落 30%——自动收敛画布真实吞吐，
  不再靠重试硬扛（实测固定 100ms 间隔时约 1s/引脚）。
- 诊断明细输出引脚 rotation 原始值，便于定位判向问题。

## 0.1.1 (2026-09-16)

实测（Spartan7 FPGA 原理图，66 引脚扇出）反馈修复：

- **引脚朝向重写**：v0.1.0 按外接盒中心主轴判定，又高又宽的符号会把左列上部引脚
  误判为上向、下部误判为下向。改为**引脚场边缘归属**（器件全部引脚坐标点云的
  min/max 边）+ 角点歧义按"边上引脚多数派"消解 + API 外接盒边缘/盒中心定侧 +
  主轴兜底；单列/单行点云（连接器、双脚器件）整侧定方向。
- **修复斜线导线**：v0.1.0 对导线终点两个坐标都做网格吸附，引脚落在 5 偏移网格
  （如 x=205、y=785）时产出斜线（[205,785]→[180,790]）。现在垂直于引出方向的
  坐标严格保持引脚原值，导线恒为水平/垂直正交线。
- **修复批量创建失败**（实测前 12 个成功、其余 54 个失败，疑似画布事务限流）：
  相邻创建间隔默认 100ms（设置页可调），失败自动退避重试（200ms×次数，至多 3 次），
  完成对话框列出失败明细，重跑本命令可补齐（已完成引脚自动跳过）。

## 0.1.0 (2026-09-16)

首个版本。

- 选中原理图器件，一键为所有尚未有网络的引脚创建以引脚名命名的网络（导线带网络名；专业版网络标签为虚拟图元——即导线名称属性，画布原生显示；v4+ 客户端可用 BETA `createNetLabel` 精确定位，亦可选网络端口）。
- 引脚朝向按器件外接盒自动判定（四边引脚分别向左/右/上/下引出），导线起点精确落在引脚端点保证电气连接。
- 忽略名单（默认含常见电源/地/NC，支持 `*` 通配）、网络名前缀/后缀。
- 长引脚名按分隔符取段（分隔符可设，段序号支持负数与钳位）。
- 放置方式：直接放在引脚上 / 跟随鼠标（标签列锚定命令触发时的鼠标位置，成列对齐）。
- 已有网络判定：Protel2 网表解析 + 已有导线端点几何贴合，双保险。
- 预览、一键清理本插件创建的导线/端口、诊断明细输出到控制台。
- 离线测试套件与打包核验脚本。
